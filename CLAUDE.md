# CLAUDE.md

Guidance for Claude Code when working in this repository.

## Project

`entp4332` — <TODO: one sentence on what this project is>.

- **Stack:** <TODO: e.g. Next.js + TypeScript, plain HTML/CSS/JS, etc.>
- **Hosting:** Vercel
- **Repo:** https://github.com/OsiK20/entp4332 (private)

## Environment

Tooling is installed via Homebrew at `/opt/homebrew` and is on `PATH` through `~/.zprofile`.

- `git` — Apple Git 2.50
- `gh` — GitHub CLI, authenticated as `OsiK20`
- `vercel` — Vercel CLI, authenticated as `dal725927-8285`
- `node` v26 + `npm` (installed as a Homebrew dependency of `vercel`)

Commit identity for this repo: `OsiK20 <dal725927@utdallas.edu>`.

## Common commands

<TODO: fill in once the stack exists, e.g.:>

```bash
# npm install          # install dependencies
# npm run dev          # local dev server
# npm run build        # production build
# npm test             # run tests
```

## Deploy

```bash
vercel            # preview deployment
vercel --prod     # production deployment
```

## Conventions

<TODO: code style, directory layout, branching, anything Claude should know>
